specVersion: "v1"
serviceId: "daily-reflection-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "daily-reflection-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
